# Optional: Custom API Key (Gift Code) Configuration
#
# These environment variables allow you to create a custom "gift code" that users
# can enter instead of their own OpenAI API key. When a user enters the custom key name,
# the backend will automatically use the custom key value for API calls.
#
# Example use case: Share "SORA-GIFT-2024" with friends, which maps to your actual API key
#
# CUSTOM_API_KEY_NAME=SORA-GIFT-2024
# CUSTOM_API_KEY_VALUE=sk-your-actual-openai-api-key-here
#
# If these are not set, users must provide their own OpenAI API keys
